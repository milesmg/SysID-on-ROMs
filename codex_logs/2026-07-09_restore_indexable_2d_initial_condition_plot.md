### ADJUSTED: Restore indexable 2D initial-condition plotting.

Files edited:
- `Research_Code/Optimization/Local/test_ROM_stability.ipynb`: added `initial_condition_2d_examples` and `initial_condition_2d_index`, then changed the plot cell to index into the 2D-only list instead of hard-coding a named filter.
